apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
data:
  application.properties: |
    quarkus.http.cors=true
    quarkus.http.cors.origins=https://staging.contestsubmission.simulatan.me,/https://.*\\\\.contestsubmission\\\\.pages\\\\.dev/,http://localhost:5173
    websiteUrl=https://invites.contestsubmission.pages.dev

